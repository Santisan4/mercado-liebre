<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Login</title>
        <link rel="stylesheet" href="/css/login-style.css">
    </head>
    <%- include("../partials/head.ejs") %>
    <body>
        <%- include("../partials/header.ejs") %>
        <div class="columns is-centered" style="background-color: #ebebeb">
            <div class="column is-6">
                <h1>Iniciar Sesion</h1>

                <% if (typeof errors != 'undefined') { %>
                        <p style="color: red">ERROR</p>
                        <ul>
                            <% for(let i = 0; i < errors.length; i++) { %>
                                <li>
                                    <%= errors[i].msg %>
                                </li>
                            <% } %>
                        </ul>
                <% } %>
                         
                <form method="POST" action="/users/login">

                    <div class="mb-3">
                        <label for="email">Email: </label>
                        <input  
                            type="text" 
                            name="email"
                            class="form-control <%= locals.errors && errors.email ? 'is-invalid' : 'null' %>">
                        <% if (locals.errors && errors.email) { %>
                        <div class="invalid-feedback">
                            <%= errors.email.msg %>
                        </div>
                        <% } %> 
                    </div>

                    <div class="mb-3">
                        <label for="password">Contraseña: </label>
                        <input 
                            type="password" 
                            name="password"
                            class="form-control <%= locals.errors && errors.password ? 'is-invalid' : 'null' %>">
                        <% if (locals.errors && errors.password) { %>
                        <div class="invalid-feedback">
                            <%= errors.password.msg %>
                        </div>
                        <% } %> 
                    </div>

                    <div class="mb-3">
                        <label for="recordame">Recordame: </label>
                        <input type="checkbox" name="recordame">
            
                    </div>

                    <div class="mb-3">
                        <button class="btn btn-primary" type="submit" >Ingresar</button>
                    </div>
                    <div>
                        <p>¿Eres nuevo? </p>
                        <p><a href="/users/register">Crea tu cuenta de Tienda Golf Eos</a></p>
                    </div>
                    
                </form>
            </div>
        </div>
    
        <div class = "ultima-parte-footer">

            <br>
            <p class = "copyright-footer">Copyright © Tienda Golf Eos S.R.L.</p>
            
        </div>
    </body>
</html>